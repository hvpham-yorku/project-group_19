{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    // Disable or modify rules here
    "@typescript-eslint/prefer-as-const": "off", // Disable 'prefer-as-const' rule globally
    "@typescript-eslint/no-unused-vars": ["warn", { "argsIgnorePattern": "^_" }], // Warn instead of error, ignore variables prefixed with "_"
    "@typescript-eslint/no-explicit-any": "off" // Disable restriction on 'any' type
  }
}
